[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn web_server_with_agent:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120 --access-logfile - --error-logfile - --preload"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[healthcheck]
path = "/health"
timeout = 10
